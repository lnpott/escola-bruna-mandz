name: New MCP server
version: 0.0.1
schema: v1
mcpServers:
  - name: New MCP server
    command: npx
    args:
      - -y
      - <your-mcp-server>
    env: {}

mcpServers:
  seu-servidor-mcp:
    command: npx
    args:
      - -y
      - "@modelcontextprotocol/server-filesystem" # Exemplo: servidor de sistema de arquivos
    env:
      # Adicione variáveis de ambiente necessárias aqui
      # EXEMPLO_API_KEY: "sua-chave-aqui"