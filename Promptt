PROMPT PARA AGENTE — Limpeza e correção da branch REFAC

Projeto: escola-bruna-mandz | Branch: REFAC


CONTEXTO

Este repositório é um sistema de gestão para escola de música (Supabase + Vercel serverless + React SPA).
A branch REFAC contém uma refatoração que nunca foi deployada porque estourou o limite de 12 Serverless Functions do plano Hobby da Vercel.

Estado atual da REFAC:


api/*.js tem 12 arquivos → todos viram Serverless Functions → estoura o limite de 12 da Vercel
3 desses arquivos são código morto do Mercado Pago (integração foi removida intencionalmente — o checkout virou "só registrar pedido como pending, pagamento combinado fora do sistema")
Existem dois conjuntos de módulos internos paralelos que precisam ser unificados: api/_lib/admin/ (conectado ao roteador) e api/_lib/financial/ (mais completo, com bugs corrigidos, mas ainda não conectado)



TAREFA

Execute as etapas abaixo em ordem, sem pular nenhuma. Ao final, registre a etapa no painel_registro.md.


ETAPA 1 — Remover arquivos mortos do Mercado Pago

Apague os seguintes arquivos (são código morto — nada no frontend os chama, confirmado por busca em todo o repositório):

api/webhook.js
api/verify-mp-payment.js
api/config.js

Remova também a dependência do package.json:

json"mercadopago": "^2.0.0"

Após remover, confirme que api/*.js tem exatamente 9 arquivos:


admin-financial.js
admin-orders.js
admin-products.js
create-payment.js
notify-new-order.js
order-status.js
products.js
update-order-status.js
upload-image.js



ETAPA 2 — Substituir api/_lib/admin/ por api/_lib/financial/

O conjunto api/_lib/financial/ é mais completo e correto que api/_lib/admin/. Diferenças importantes:


financial/investments.js tem PATCH e DELETE (admin só tinha GET/POST — pendência conhecida)
financial/students.js usa status como única fonte de verdade (sem students.active que foi removido do schema)
financial/helpers.js tem safeFloat, safeInt, parsePagination, normalizeOptionalFields (admin não tinha esses helpers)
financial/dashboard.js corrige bug onde active_teachers contava todos os professores sem filtrar por active = true
financial/ tem testes unitários cobrindo helpers e handler de students (tests/financial-helpers.test.js, tests/financial-students.test.js)


2a. Apague a pasta inteira:

api/_lib/admin/
api/_lib/admin-auth.js

2b. Reescreva api/admin-financial.js para importar de _lib/financial/ em vez de _lib/admin/:

js/**
 * api/admin-financial.js
 * Roteador fino da API financeira do painel admin.
 * Lógica de cada resource em api/_lib/financial/*.js
 *
 * Continua sendo UMA única Serverless Function de propósito:
 * plano Hobby da Vercel tem limite de 12 funções por deploy.
 * Arquivos em api/_lib/ não contam como funções.
 */

import { getSupabase } from './_lib/supabase.js';
import { checkAdminAuth } from './_lib/admin-auth.js';
import { handleStudents }       from './_lib/financial/students.js';
import { handleTeachers }       from './_lib/financial/teachers.js';
import { handleEnrollments }    from './_lib/financial/enrollments.js';
import { handleTuitions }       from './_lib/financial/tuitions.js';
import { handlePayments }       from './_lib/financial/payments.js';
import { handleExpenses }       from './_lib/financial/expenses.js';
import { handleInvestments }    from './_lib/financial/investments.js';
import { handleTeacherPayments } from './_lib/financial/teacherPayments.js';
import { handleSummary }        from './_lib/financial/summary.js';
import { handleDashboard }      from './_lib/financial/dashboard.js';
import { handleLessons }        from './_lib/financial/lessons.js';
import { handleAttendance }     from './_lib/financial/attendance.js';

export default async function handler(req, res) {
    if (!checkAdminAuth(req, res)) return;

    let supabase;
    try {
        supabase = getSupabase();
    } catch (err) {
        console.error('Supabase init error:', err);
        return res.status(500).json({ error: 'Supabase não configurado.' });
    }

    const { resource } = req.query;

    try {
        switch (resource) {
            case 'students':         return await handleStudents(req, res, supabase);
            case 'teachers':         return await handleTeachers(req, res, supabase);
            case 'enrollments':      return await handleEnrollments(req, res, supabase);
            case 'tuitions':         return await handleTuitions(req, res, supabase);
            case 'payments':         return await handlePayments(req, res, supabase);
            case 'expenses':         return await handleExpenses(req, res, supabase);
            case 'investments':      return await handleInvestments(req, res, supabase);
            case 'teacher_payments': return await handleTeacherPayments(req, res, supabase);
            case 'summary':          return await handleSummary(req, res, supabase);
            case 'dashboard':        return await handleDashboard(req, res, supabase);
            case 'lessons':          return await handleLessons(req, res, supabase);
            case 'attendance':       return await handleAttendance(req, res, supabase);
            default:
                return res.status(400).json({
                    error: 'Parâmetro ?resource= inválido ou ausente. Use: students, teachers, enrollments, tuitions, payments, expenses, investments, teacher_payments, summary, dashboard, lessons, attendance.'
                });
        }
    } catch (err) {
        console.error(`[admin-financial] resource=${resource}`, err);
        return res.status(500).json({ error: 'Erro interno.' });
    }
}

2c. Crie api/_lib/admin-auth.js (o único arquivo de _lib/admin/ que precisa sobreviver — é a autenticação compartilhada):

js/**
 * api/_lib/admin-auth.js
 * Verifica o header x-admin-password em todas as rotas do painel admin.
 */

export function checkAdminAuth(req, res) {
    const adminPassword = process.env.ADMIN_PASSWORD;
    if (!adminPassword) {
        res.status(500).json({ error: 'ADMIN_PASSWORD não configurado.' });
        return false;
    }
    const provided = req.headers['x-admin-password'];
    if (provided !== adminPassword) {
        res.status(401).json({ error: 'Senha incorreta.' });
        return false;
    }
    return true;
}


ETAPA 3 — Validar sintaxe de todos os arquivos JS alterados

Execute:

bashnode --check api/admin-financial.js
node --check api/_lib/admin-auth.js
node --check api/_lib/financial/students.js
node --check api/_lib/financial/teachers.js
node --check api/_lib/financial/enrollments.js
node --check api/_lib/financial/tuitions.js
node --check api/_lib/financial/payments.js
node --check api/_lib/financial/expenses.js
node --check api/_lib/financial/investments.js
node --check api/_lib/financial/teacherPayments.js
node --check api/_lib/financial/summary.js
node --check api/_lib/financial/dashboard.js
node --check api/_lib/financial/lessons.js
node --check api/_lib/financial/attendance.js
node --check api/_lib/financial/helpers.js

Todos devem passar sem erro.


ETAPA 4 — Rodar os testes existentes

bashnpm install
npm test

Esperado: 3 suites passando (financial-helpers, financial-students, webhook-signature).


ETAPA 5 — Confirmar contagem de Serverless Functions

bashls api/*.js | grep -v "^api/_"

Deve listar exatamente 9 arquivos. Se listar mais de 12, o deploy na Vercel falhará.


ETAPA 6 — Build de produção

bashnpm run build

Deve completar sem erros. Warnings de tamanho de bundle são aceitáveis.


ETAPA 7 — Busca final por referências mortas

Confirme que não sobrou nenhuma referência ao Mercado Pago:

bashgrep -r "mercadopago\|MercadoPago\|verify-mp-payment\|api/config\|MERCADO_PAGO_PUBLIC_KEY\|payment-brick\|MP_WEBHOOK" api/ app/ store/ index.html --include="*.js" --include="*.ts" --include="*.tsx" --include="*.html" 2>/dev/null

Resultado esperado: nenhuma linha (output vazio).


ETAPA 8 — Registrar no painel_registro.md

Adicione ao final do painel_registro.md (antes da seção "OBSERVAÇÕES FINAIS") a seguinte etapa, preenchendo data/horário/commit reais:

markdown---

# ETAPA 42 — LIMPEZA REFAC: REMOÇÃO DO MP + UNIFICAÇÃO DE MÓDULOS

**Data:** [data real]
**Horário:** [horário real]
**Agente Responsável:** [nome do agente]
**Commit Git:** [hash do commit]

---

## Objetivo

Resolver os dois problemas que impediam o deploy da branch REFAC na Vercel:
1. Contagem de Serverless Functions acima do limite de 12 do plano Hobby (eram 12, limite é 12 — sem margem)
2. Dois conjuntos de módulos internos paralelos (`api/_lib/admin/` e `api/_lib/financial/`) causando confusão e duplicação

---

## Implementações Realizadas

- **Removidos** `api/webhook.js`, `api/verify-mp-payment.js`, `api/config.js` — código morto do Mercado Pago (integração removida na Etapa 40; esses arquivos voltaram por extração de zip por cima do repositório)
- **Removida** dependência `mercadopago` do `package.json`
- **Removida** pasta `api/_lib/admin/` inteira (9 módulos + shared.js) — substituída por `api/_lib/financial/` que é mais completo
- **`api/admin-financial.js`** reescrito para importar de `api/_lib/financial/` — agora cobre 12 resources (students, teachers, enrollments, tuitions, payments, expenses, investments, teacher_payments, summary, dashboard, lessons, attendance)
- **Mantido** `api/_lib/admin-auth.js` como único remanescente de `_lib/admin/` (autenticação compartilhada)
- Contagem de Serverless Functions: de 12 → 9

## Melhorias trazidas pelo _lib/financial/ em relação ao _lib/admin/:

- `investments`: CRUD completo (antes só GET/POST)
- `students`: usa `status` como única fonte de verdade (sem `students.active` removido no schema)
- `dashboard`: corrige contagem de `active_teachers` (antes contava todos, sem filtrar por `active = true`)
- `helpers`: `safeFloat`, `safeInt`, `parsePagination`, `normalizeOptionalFields` (não existiam em admin/)
- Cobertura de testes unitários real (`tests/financial-helpers.test.js`, `tests/financial-students.test.js`)

---

## Arquivos Alterados

- `api/webhook.js` — **removido**
- `api/verify-mp-payment.js` — **removido**
- `api/config.js` — **removido**
- `api/_lib/admin/` — **pasta removida**
- `api/_lib/admin-auth.js` — recriado standalone (mantido)
- `api/admin-financial.js` — reescrito (importa de _lib/financial/)
- `package.json` — dependência `mercadopago` removida

---

## Alterações no Banco

Nenhuma.

---

## Testes

✅ `node --check` em todos os módulos JS alterados
✅ `npm test` — 3 suites passando
✅ `npm run build` — build sem erros
✅ Nenhuma referência ao Mercado Pago restante (grep vazio)
✅ Contagem de Serverless Functions: 9 (dentro do limite de 12 do plano Hobby)

---

## Pendências

- Deploy real na Vercel a partir da branch REFAC e testes funcionais ponta a ponta
- Merge da REFAC na main após validação em produção
- Variáveis de ambiente na Vercel: `MERCADO_PAGO_ACCESS_TOKEN`, `MERCADO_PAGO_PUBLIC_KEY`, `MP_WEBHOOK_URL` não são mais usadas — podem ser removidas quando conveniente

---

## Próxima Etapa

Deploy da REFAC na Vercel, validação funcional ponta a ponta (fluxo: vínculo → aula → presença → financeiro), e merge para main.


NOTAS IMPORTANTES PARA O AGENTE


NÃO toque em commercial/index.html — é o painel da loja, fora do escopo
NÃO toque em painel-x9k2f.html — é só o portal de login, está correto
NÃO crie novos arquivos em api/ diretamente — qualquer novo arquivo em api/*.js vira Serverless Function e pode estourar o limite
NÃO faça push direto na main — todo o trabalho fica na branch REFAC até validação em produção
O arquivo api/create-payment.js deve ser mantido — é o endpoint de criação de pedido (fluxo manual, sem MP)
O arquivo api/notify-new-order.js deve ser mantido — envia e-mail via Resend quando pedido é aprovado manualmente